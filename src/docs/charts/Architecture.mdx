import { Meta } from "@storybook/blocks";
import { AreaChart, Badge, BarChart, Card, DonutChart, LineChart, Section, Tabs } from "../../index";
import { componentUsageData, salesData, themeShareData } from "../../mocks/charts";
import { CodeBlock, DocsPage, PropsTable } from "../internal";

<Meta title="Charts/Architecture" />

<DocsPage
  title="Charts Architecture"
  status="experimental"
  description="Graficos autorais feitos com SVG, React e TypeScript, sem Recharts, Chart.js, ApexCharts ou bibliotecas externas de charts."
>
  <Section title="Preview" description="Os exemplos abaixo usam tokens do tema atual da pagina.">
    <div className="ak-docs-grid">
      <Card title="LineChart" variant="outlined"><LineChart data={salesData} height={180} showTooltip xKey="month" yKey="value" /></Card>
      <Card title="BarChart" variant="outlined"><BarChart data={componentUsageData} height={180} showTooltip xKey="name" yKey="value" /></Card>
      <Card title="DonutChart" variant="outlined"><DonutChart data={themeShareData} height={180} labelKey="theme" valueKey="value" /></Card>
    </div>
  </Section>

  <Section title="Estrutura">
    <CodeBlock language="txt" title="Arquitetura">
{`src/charts/
  core/
    ChartFrame.tsx
    types.ts
  utils/
    paths.ts
    scales.ts
  components/
    LineChart.tsx
    BarChart.tsx
    AreaChart.tsx
    PieChart.tsx
    DonutChart.tsx
    Sparkline.tsx`}
    </CodeBlock>
  </Section>

  <Section title="Core e Utils">
    <Tabs
      items={[
        {
          id: "core",
          label: "Core",
          content: (
            <Card title="ChartFrame" description="Centraliza superficie, loading state, empty state, legenda e tooltip ancorado." variant="outlined">
              <Badge variant="info">shared behavior</Badge>
            </Card>
          ),
        },
        {
          id: "utils",
          label: "Utils",
          content: (
            <PropsTable
              rows={[
                { name: "createLinearScale", type: "function", description: "Cria escala numerica simples." },
                { name: "getValueExtent", type: "function", description: "Calcula minimo e maximo dos valores." },
                { name: "getCoordinates", type: "function", description: "Transforma dados em coordenadas SVG." },
                { name: "createLinePath", type: "function", description: "Gera path SVG para linhas." },
                { name: "createAreaPath", type: "function", description: "Gera path SVG para areas." },
                { name: "createArcPath", type: "function", description: "Gera path SVG para pie e donut." },
              ]}
            />
          ),
        },
      ]}
    />
  </Section>

  <Section title="Customizacao por Grafico">
    <PropsTable
      rows={[
        { name: "colors", type: "ChartColors", description: "Override manual de line, area, bar, grid, text e donut." },
        { name: "height / width", type: "number", description: "Controle do viewBox." },
        { name: "showGrid", type: "boolean", description: "Grid horizontal." },
        { name: "showXAxis / showYAxis", type: "boolean", description: "Labels basicos de eixo." },
        { name: "showTooltip", type: "boolean", description: "Tooltip ancorado ao elemento ativo." },
        { name: "renderTooltip", type: "(payload) => ReactNode", description: "Conteudo customizado para o tooltip." },
        { name: "tooltipStyle", type: "CSSProperties", description: "Override visual do tooltip." },
        { name: "showLegend", type: "boolean", description: "Legenda." },
        { name: "lineWidth / radius / fill", type: "number | boolean", description: "Ajustes visuais por tipo de grafico." },
      ]}
    />
  </Section>

  <Section title="Tooltip">
    <Card title="Comportamento" description="O tooltip fica proximo ao ponto, barra ou fatia em hover/focus e evita sair da area visivel." variant="outlined">
      <AreaChart data={salesData} height={180} showTooltip xKey="month" yKey="value" />
    </Card>
    <CodeBlock title="Tooltip customizado">
{`<LineChart
  data={salesData}
  xKey="month"
  yKey="value"
  showTooltip
  renderTooltip={(payload) => (
    <div>
      <strong>{payload.label}</strong>
      <div>{payload.value} vendas</div>
    </div>
  )}
  tooltipStyle={{
    background: "#111827",
    borderRadius: 10,
    boxShadow: "0 16px 40px rgba(17, 24, 39, 0.22)",
    color: "#ffffff",
  }}
/>`}
    </CodeBlock>
  </Section>
</DocsPage>
