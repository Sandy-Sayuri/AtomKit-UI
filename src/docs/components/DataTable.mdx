import { Meta } from "@storybook/blocks";
import { Badge, DataTable, Section } from "../../index";
import { CodeBlock, DocsPage, PropsTable } from "../internal";

<Meta title="Components/DataTable" />

<DocsPage title="DataTable" status="stable" description="Organismo para renderizacao tabular generica, sem regra de negocio acoplada.">
  <Section title="Exemplo Visual">
    <DataTable
      bordered
      striped
      selectable
      columns={[
        { header: "Nome", key: "name", sortable: true },
        { header: "Area", key: "role" },
        { header: "Status", key: "status", render: (row) => <Badge variant={row.status === "Ativo" ? "success" : "warning"}>{row.status}</Badge> },
      ]}
      data={[
        { name: "Ana Silva", role: "Design System", status: "Ativo" },
        { name: "Bruno Lima", role: "Frontend", status: "Pendente" },
      ]}
    />
  </Section>

  <Section title="Props Principais">
    <PropsTable
      rows={[
        { name: "columns", type: "DataTableColumn<T>[]", required: true, description: "Configuracao das colunas." },
        { name: "data", type: "T[]", required: true, description: "Dados renderizados." },
        { name: "loading", type: "boolean", description: "Mostra estado de carregamento." },
        { name: "emptyState", type: "ReactNode", description: "Conteudo para tabela vazia." },
        { name: "rowActions", type: "DataTableAction<T>[]", description: "Acoes por linha." },
        { name: "selectable", type: "boolean", description: "Habilita selecao." },
        { name: "density", type: '"compact" | "comfortable"', description: "Densidade visual." },
        { name: "striped / bordered", type: "boolean", description: "Estilos de tabela." },
      ]}
    />
  </Section>

  <CodeBlock title="Uso">
{`const columns = [
  { key: "name", header: "Nome", sortable: true },
  { key: "role", header: "Area" },
];

<DataTable
  columns={columns}
  data={users}
  selectable
  striped
  bordered
/>`}
  </CodeBlock>
</DocsPage>
