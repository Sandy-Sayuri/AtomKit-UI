import { Meta } from "@storybook/blocks";
import { Alert, Button, Card, FormField, Section, Tabs } from "../index";
import { CodeBlock, DocsPage } from "./internal";

<Meta title="Docs/Getting Started" />

<DocsPage
  title="Getting Started"
  status="planned"
  description="Uso esperado da AtomKit UI quando o pacote estiver pronto para consumo externo. O projeto ainda nao foi publicado no npm."
>
  <Alert title="Publicacao futura" variant="warning">
    A biblioteca ainda nao deve ser publicada no npm. Os exemplos abaixo documentam o contrato de uso planejado.
  </Alert>

  <Section title="Fluxo Basico" description="Instalar, importar estilos, aplicar provider e compor componentes.">
    <Tabs
      items={[
        {
          id: "install",
          label: "Instalacao",
          content: <CodeBlock language="bash" title="Instalacao futura">{`npm install atomkit-ui`}</CodeBlock>,
        },
        {
          id: "import",
          label: "Importacao",
          content: (
            <CodeBlock title="Imports">
{`import { AtomKitProvider, Button, Card, FormField } from "atomkit-ui";
import "atomkit-ui/styles";`}
            </CodeBlock>
          ),
        },
        {
          id: "provider",
          label: "Provider",
          content: (
            <CodeBlock title="Provider de tema">
{`<AtomKitProvider theme="dark">
  <Button>Acao</Button>
</AtomKitProvider>`}
            </CodeBlock>
          ),
        },
      ]}
    />
  </Section>

  <Section title="Exemplo Visual" description="O mesmo padrao documentado e renderizado com componentes reais.">
    <Card title="Usuario" description="Dados basicos" footer={<Button variant="primary">Salvar</Button>} variant="outlined">
      <FormField label="Nome" placeholder="Digite seu nome" required />
    </Card>
  </Section>

  <Section title="Override Parcial">
    <CodeBlock title="Tokens customizados">
{`<AtomKitProvider
  theme="light"
  tokens={{
    colors: {
      primary: "#be123c",
      background: "#fff7f8",
    },
    radius: "14px",
    density: "compact",
    fontFamily: "Inter, sans-serif",
  }}
>
  <Button>Acao customizada</Button>
</AtomKitProvider>`}
    </CodeBlock>
  </Section>
</DocsPage>
